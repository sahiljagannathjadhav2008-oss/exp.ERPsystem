<?xml version="1.0" encoding="utf-8"?>
<!--
    Backups of this app's data are handled explicitly by the in-app
    Backup/Restore engine (encrypted, user-initiated export to a location the
    user chooses). We deliberately exclude everything from Android's
    automatic cloud backup so financial data is never silently uploaded.
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
    </device-transfer>
</data-extraction-rules>
